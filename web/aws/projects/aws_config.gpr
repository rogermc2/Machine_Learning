abstract project AWS_Config is
   for Source_Dirs use ();

   type Boolean_Type is ("true", "false");
   Zlib_Exists : Boolean_Type := "true";

   type SOCKET_Type is ("std", "openssl", "gnutls");
   SOCKET : SOCKET_Type := "std";

end AWS_Config;
