with "../aws/ssl/ssl.gpr";

--  with "../aws/aws";

project Download_Manager is

   for Source_Dirs use ("../aws/core", "../aws/templates_parser/src", "../aws/extended", "../aws/include", "../aws/ssl", "../aws/http2", "../aws/config/**", "src");
   for Main use ();
   for Object_Dir use "obj";
   for Exec_Dir use ".";

   package Builder is
      for Default_Switches ("ada") use ("-gnat12");
      for Switches ("ada") use ("-gnat12");
   end Builder;

end Download_Manager;

