with "utilada_http.gpr";
with "gnatcoll.gpr";

project MLP_Alpha_Test is

   for Source_Dirs use ("src", "../../../datasets", "../../../neural_network", "../../../decision_tree_classifier/support", "../../../common", "../../../utils", "../../../export", "../../../../../../usr/local/include/plplot", "../../../model_selection", "../../../base", 
      "../../../metrics", "../../../test");
   for Object_Dir use "obj";
   for Main use ("test_alpha");
   for Exec_Dir use ".";

   package Compiler is
      for Default_Switches ("ada") use ("-O2", "-fstack-check", "-gnatwkmru", "-gnatyclux");
   end Compiler;

   package Linker is
      for Switches ("ada") use ("-L/usr/local/lib", "-lplplot");
   end Linker;

end MLP_Alpha_Test;

