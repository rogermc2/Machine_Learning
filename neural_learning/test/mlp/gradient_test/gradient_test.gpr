with "utilada_http.gpr";
with "gnatcoll.gpr";

project Gradient_Test is

   for Source_Dirs use ("src", "../../../datasets", "../../../neural_network", "../../../decision_tree_classifier/support", "../../../common", "../../../utils", "../../../export", "../../../model_selection", "../../../base", 
      "../../../metrics", "../../../test", "../../../../scipy", "../../../../scipy/optimise");
   for Object_Dir use "obj";
   for Main use ("test_gradient");
   for Exec_Dir use ".";

   package Compiler is
      for Default_Switches ("ada") use ("-O2", "-fstack-check", "-gnatwkmru", "-gnatyclux");
   end Compiler;

   package Linker is
      for Switches ("ada") use ("-L/usr/local/gfortran/lib", "-lgfortran", "-llapack", "-lblas");
   end Linker;

   for Languages use ("Ada", "Fortran");

   package Naming is
      for Body_Suffix ("Fortran") use ".f";
   end Naming;

end Gradient_Test;

