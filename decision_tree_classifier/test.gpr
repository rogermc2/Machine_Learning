project Test is

   for Source_Dirs use ("test/**", "support", "utils", "../common");
   for Object_Dir use "obj";
   for Exec_Dir use ".";
   for Main use ("test_functions.adb");

   package Compiler is
      for Default_Switches ("ada") use ("-gnatyclux");
   end Compiler;

end Test;

